export interface University {
  id?: number;
  name: string;
  email: string;
  address: string;
  taxCode: string;
  website: string;
  logo: string;
  createdAt?: string;
  updatedAt?: string;
  locked?: boolean;
  verifile?: boolean;
}
